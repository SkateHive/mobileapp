import type { LucideIcon } from 'lucide-react-native';

// Simple icon utility without NativeWind dependency
export function iconWithClassName(icon: LucideIcon) {
  // This function can be removed or simplified since we're not using className anymore
  return icon;
}
